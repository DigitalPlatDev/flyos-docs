# Service Manager
## Boot Manager
In the container directory `/boot/scripts`, the shell scripts within this directory will run when the FlyOS container service starts. It is recommended to use `nohup` to ensure the scripts always run in the background.